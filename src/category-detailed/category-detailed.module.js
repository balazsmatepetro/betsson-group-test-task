import angular from 'angular';
import component from './category-detailed.component';

export default angular
    .module('app.category-detailed', [])
    .component('categoryDetailed', component)
    .name;
